import styled from "@emotion/styled";
import { motion } from "framer-motion";

export const OrchestratorContainer = styled(motion.div)`
  user-select: none;
  img {
    width: 150px;
    border-radius: 50%;
  }
`;
